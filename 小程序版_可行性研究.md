# 《上岸模拟器》微信小程序版 · 可行性研究与迁移方案

> 更新：2026-08-03

---

## 一、可行性评估

### 1.1 技术可行性：✅ 高度可行

| Web版技术 | 小程序对应方案 | 迁移难度 |
|-----------|--------------|---------|
| 原生JS | 小程序JS（WXS） | ⭐ 低 |
| HTML | WXML | ⭐⭐ 中（语法不同） |
| CSS | WXSS | ⭐ 低（大部分兼容） |
| localStorage | wx.setStorageSync / wx.getStorageSync | ⭐ 低（API映射） |
| DOM操作(document.getElementById) | 数据绑定(data + setData) | ⭐⭐⭐ 高（核心改造点） |
| onclick事件 | bindtap | ⭐ 低 |
| innerHTML模板 | WXMLMustache语法 | ⭐⭐ 中 |
| 腾讯混元AI API | wx.request | ⭐ 低 |

### 1.2 核心改造点

**最大工作量**：DOM操作 → 数据绑定

Web版大量使用 `document.getElementById` + `innerHTML` 来渲染UI。小程序无法操作DOM，必须改为：
- `Page({ data: {...} })` 管理状态
- `this.setData({...})` 触发渲染
- WXML `{{}}`Mustache语法绑定数据

### 1.3 存储映射

| Web | 小程序 |
|-----|--------|
| localStorage.getItem(key) | wx.getStorageSync(key) |
| localStorage.setItem(key, val) | wx.setStorageSync(key, val) |
| localStorage.removeItem(key) | wx.removeStorageSync(key) |

存储上限10MB（Web版localStorage通常5MB），完全够用。

---

## 二、迁移策略

### 2.1 渐进式迁移（推荐）

不重写整个游戏，而是分层迁移：

```
Phase 1: 数据层（直接复用）
  data.js → data.js（几乎不改，去掉const声明改为module.exports）

Phase 2: 存储层（API映射）
  SaveSystem → wx.setStorageSync 封装

Phase 3: 逻辑层（核心复用）
  Game对象的方法几乎全部保留，只改DOM调用为setData

Phase 4: UI层（重写）
  index.html → 多个WXML页面
  style.css → app.wxss + page.wxss
```

### 2.2 页面拆分

Web版是单页面SPA，小程序拆为多页面：

| Web区域 | 小程序页面 |
|---------|-----------|
| #screen-start | pages/start/index |
| #screen-identity | pages/identity/index |
| #screen-province | pages/province/index |
| #screen-lifetags | pages/lifetags/index |
| #screen-month | pages/month/index |
| #screen-game | pages/game/index（主游戏页） |
| #screen-ending | pages/ending/index |
| 事件模态 | pages/event/index 或组件 |

---

## 三、项目结构

```
miniprogram/
├── app.js              # 小程序入口
├── app.json            # 全局配置
├── app.wxss            # 全局样式
├── data/
│   └── data.js         # 从Web版迁移（module.exports）
├── utils/
│   ├── game.js         # 核心逻辑（从Web版迁移，改DOM为setData）
│   └── storage.js      # 存储封装
├── pages/
│   ├── start/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.js
│   ├── identity/
│   ├── province/
│   ├── lifetags/
│   ├── month/
│   ├── game/
│   └── ending/
└── project.config.json
```

---

## 四、启动步骤

### 4.1 你需要做的

1. **注册小程序账号**：https://mp.weixin.qq.com/
   - 选择"小程序"
   - 获取AppID

2. **下载微信开发者工具**：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

3. **告诉我AppID**，我来创建项目骨架

### 4.2 我已完成的

- ✅ 可行性研究
- ✅ 迁移策略设计
- ✅ 项目结构规划
- ✅ 存储API映射表

### 4.3 下一步（你提供AppID后）

1. 创建小程序项目骨架（app.json + 页面结构）
2. 迁移data.js（改为module.exports）
3. 迁移存储层（localStorage → wx.setStorageSync）
4. 迁移核心逻辑层（Game对象，DOM→setData）
5. 重写UI层（HTML→WXML，CSS→WXSS）
6. 调试+真机预览

---

## 五、风险与注意事项

| 风险 | 说明 | 应对 |
|------|------|------|
| 代码包大小限制 | 主包2MB，总包20MB | data.js较大(3800行)可能需要分包加载 |
| DOM改造工作量大 | game.js 2100行大量DOM操作 | 渐进式迁移，优先改核心交互 |
| 事件模态框 | Web用innerHTML动态渲染 | 小程序用component或独立页面 |
| 分享功能 | 需要用canvas生成图片 | wx.canvasToTempFilePath |
| 审核合规 | 不能有具体公务员考试机构名称 | 已用泛称（省直机关等） |

---

## 六、预估工作量

| 阶段 | 内容 | 预估 |
|------|------|------|
| Phase 1 | 数据层迁移 | 0.5天 |
| Phase 2 | 存储层迁移 | 0.5天 |
| Phase 3 | 逻辑层迁移（核心） | 2-3天 |
| Phase 4 | UI层重写 | 2-3天 |
| 调试 | 真机测试+修bug | 1-2天 |
| **总计** | | **6-10天** |

---

*等你提供AppID和开发者工具安装完成，我立即开始创建项目骨架*
