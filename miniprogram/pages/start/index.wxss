/* pages/start/index.wxss */
.logo { text-align: center; margin: 60rpx 0; }
.logo-sub { font-size: 28rpx; color: rgba(255,255,255,0.5); }
.logo-main { font-size: 64rpx; font-weight: bold; color: #d4a574; margin: 16rpx 0; }
.logo-tag { font-size: 24rpx; color: rgba(255,255,255,0.4); }
.stats-preview { display: flex; justify-content: space-around; margin: 40rpx 0; }
.stat-item { font-size: 28rpx; }
.intro { text-align: center; margin: 40rpx 0; }
.intro text { display: block; font-size: 28rpx; color: rgba(255,255,255,0.7); margin: 8rpx 0; }
.btn-main { margin: 20rpx 0; }
.btn-secondary { margin: 20rpx 0; }
